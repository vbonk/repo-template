# Bootstrap

Use `docs/PHASE-0.md` as the canonical first-agent procedure for a repository derived from `vbonk/repo-template`.

In the source template itself, remain in source-template maintenance mode.
